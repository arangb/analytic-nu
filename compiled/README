To compile the code here, firt I had to install pybind: 
sudo apt install python3-pybind11 pybind11-dev
then:
g++ -O3 -Wall -shared -std=c++11 -fPIC $(python3 -m pybind11 --includes) \
    NuSolveLJ.cpp -o pynusolver.so $(python3-config --ldflags --embed)

